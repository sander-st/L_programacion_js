"use strict";
const imparOrPar = (num) => {
    if (num % 2 === 0)
        console.log(`El numero ${num} es par.`);
    else
        console.log(`El numero ${num} es impar.`);
};
imparOrPar(12);
imparOrPar(5);
